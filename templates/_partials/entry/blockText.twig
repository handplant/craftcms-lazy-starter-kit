{% set builder = builder ?? false %}
{% extends builder ? "_partials/matrix.twig" : "_partials/cke.twig" %}
{% set classWrapper = builder ? 'blocks blocks--text' : 'inlines inlines--text' %}
{% set classContent = builder ? 'blocks__content' : '' %}

{% block content %}
  <div class="prose md:prose-lg lg:prose-xl">
    {{ entry.text }}
  </div>
{% endblock %}
