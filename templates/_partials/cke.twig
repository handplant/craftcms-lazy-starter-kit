{% set anchorId = entry.anchor.anchorTitle ?? null %}

<div
  {% if classWrapper %}class="{{ classWrapper }} not-prose"{% endif %}
  {% if anchorId %}id="{{ anchorId|kebab }}"{% endif %}
>
  <div class="{{ classContent ?? '' }}">
    {% block content %}
    {% endblock %}
  </div>
</div>
